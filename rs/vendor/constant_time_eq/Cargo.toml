[package]
name = "constant_time_eq"
version = "0.4.2"
edition = "2021"
license = "CC0-1.0"
description = "Compares two equal-sized byte strings in constant time."

[lib]
path = "src/lib.rs"

[features]
default = ["std"]
std = []

[dependencies]
